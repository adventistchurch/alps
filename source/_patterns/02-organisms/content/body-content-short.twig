<div class="spacing--double flex h--100p">

  <div class="content-blocks spacing--double">
    {% for block in content_blocks %}
    <div class="pad--primary spacing--half text">
      <h3 class="brown font--tertiary--l">{{ text.heading  | raw}}</h3>
      <p>{{ block.text.description | raw }} {% if block.text.cta %} <a href="{{ block.url }}" class="font--secondary--s upper"><strong>{{ block.text.cta | raw}}</strong></a> {% endif %}</p>
    </div>
    <hr>
    {% endfor %}
  </div>

  {% include '@molecules/blocks/story-block.twig' with {'block_class': 'flex--end'} %}
</div>
